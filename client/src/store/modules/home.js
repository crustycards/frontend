const initialState = {
  currentUser: null,
  friends: [],
  requestsSent: [],
  requestsReceived: []
};

export default (state = initialState, {type, payload}) => {
  switch (type) {
  default: 
    return state;
  }
};